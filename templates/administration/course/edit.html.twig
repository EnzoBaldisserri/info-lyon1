{% extends 'base.html.twig' %}

{% block body %}
    <div class="title row valign-wrapper">
        <div class="col s11">
            <h2>{{ 'course.form.edit.title'|trans }}</h2>
        </div>
        <div class="col s1">
            {{ include('administration/course/_delete_form.html.twig') }}
        </div>
    </div>

    {{ include('administration/course/_form.html.twig', {
        'button_label': 'global.button.update'|trans,
    }) }}
{% endblock %}
