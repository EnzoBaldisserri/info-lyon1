{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ 'course.form.edit.title'|trans }}</h1>

    {{ include('administration/course/_form.html.twig', {
        'button_label': 'global.button.update'|trans,
        'actions': [
            {
                'content': include('administration/course/_delete_form.html.twig')
            },
            {
                'text': 'administration.message.come_back'|trans,
                'path': path('administration_index')
            }
        ]
    }) }}
{% endblock %}
