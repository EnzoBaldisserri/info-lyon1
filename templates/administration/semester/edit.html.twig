{% extends 'base.html.twig' %}

{% block body %}
    <div class="title row valign-wrapper">
        <div class="col s11">
            <h1>{{ 'semester.form.edit.title'|trans }}</h1>
        </div>
        <div class="col s1">
            {{ include('administration/semester/_delete_form.html.twig') }}
        </div>
    </div>

    {{ include('administration/semester/_form.html.twig', {
        'button_label': 'global.button.update'|trans,
    }) }}
{% endblock %}
