<form
    method="post"
    action="{{ path('administration_semester_delete', {'id': semester.id}) }}"
    data-confirm="{{ 'semester.form.delete.confirm'|trans }}"
>
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ semester.id) }}">
    <button class="btn btn-flat waves-effect waves-red">{{ 'global.button.delete'|trans }}</button>
</form>
