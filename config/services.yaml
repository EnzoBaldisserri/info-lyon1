# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices/configuration.html#application-related-configuration
parameters:
    locale: 'fr'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
        public: false       # Allows optimizing the container by removing unused services; this also means
                            # fetching services directly from the container via $container->get() won't work.
                            # The best practice is to be explicit about your dependencies anyway.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/*'
        exclude: '../src/{Entity,Migrations,Tests,Kernel.php}'

    # controllers are imported separately to make sure services can be injected
    # as action arguments even if you don't extend any base controller class
    App\Controller\:
        resource: '../src/Controller'
        tags: ['controller.service_arguments']

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
    App\Service\NavbarBuilder:
        arguments:
            - "@security.authorization_checker"
            - ROLE_STUDENT:
                  Absence: absence_homepage
                  Notes: mark_homepage

              ROLE_TEACHER:
                  Absence: absence_homepage
                  Contrôles: control_homepage

              ROLE_SECRETARIAT:
                  Absence: absence_homepage

              ROLE_PROJECT_MEMBER:
                  Projet: project_homepage

              ROLE_FORUM_ACCESS:
                  Forum: forum_homepage

              ROLE_FOLLOW_UP:
                  Suivi: followup_homepage

              ROLE_ADMINISTRATIVE:
                  Administration: administration_homepage

              ROLE_SCHEDULED:
                  Emploi du temps: schedule_homepage

    App\Service\ScheduleFetcher:
        arguments:
          $fetch_url: "http://adelb.univ-lyon1.fr/jsp/custom/modules/plannings/anonymous_cal.jsp\
              ?resources={resources}\
              &projectId={projectId}\
              &calType=ical\
              &firstDate={firstDate}\
              &lastDate={lastDate}"
          $projectId: 1
          $date_format: 'Y-m-d'
